<div class="bg-white rounded-lg shadow-md p-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl">Create New Album</h1>
    <a href="/admin/dashboard" class="text-blue-500 hover:text-blue-600">← Back to Dashboard</a>
  </div>

  <% if @flash_messages && @flash_messages[:error] %>
    <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative mb-4" role="alert">
      <span class="block sm:inline"><%= @flash_messages[:error] %></span>
    </div>
  <% end %>

  <form method="POST" action="/admin/albums/create" class="space-y-6">
    <div>
      <label for="name" class="block text-sm font-medium text-gray-700">Album Name</label>
      <input type="text" 
             id="name" 
             name="name" 
             required
             class="mt-1 block w-full rounded-md border border-gray-300 shadow-sm p-2"
             placeholder="e.g., Nature, Urban, Portrait">
      <p class="mt-1 text-sm text-gray-500">This will be used in URLs and file paths (spaces will be converted to hyphens)</p>
    </div>

    <div>
      <label for="description" class="block text-sm font-medium text-gray-700">Description</label>
      <textarea id="description" 
                name="description" 
                rows="3" 
                required
                class="mt-1 block w-full rounded-md border border-gray-300 shadow-sm p-2"
                placeholder="Describe your album..."></textarea>
    </div>

    <div class="bg-gray-50 p-4 rounded">
      <h3 class="text-sm font-medium text-gray-700 mb-2">Album Structure Preview</h3>
      <pre class="text-xs text-gray-600 bg-gray-100 p-4 rounded overflow-x-auto">
/images/
  ├── albums/
  │   └── [album-name]/
  │       └── (your images will go here)
  └── [album-name]/
      └── index.html (with gallery template)
      </pre>
    </div>

    <button type="submit" 
            class="w-full bg-blue-500 text-white px-6 py-3 rounded-md hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-offset-2">
      Create Album
    </button>
  </form>
</div>